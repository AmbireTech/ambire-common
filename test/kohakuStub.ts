/**
 * Stands in for the Railgun SDK packages under Jest - see the `moduleNameMapper` note in
 * jest.config.js. Deliberately empty: any test that actually needs the SDK's behaviour would have
 * to load the WASM module, which Jest cannot do.
 */
export {}
