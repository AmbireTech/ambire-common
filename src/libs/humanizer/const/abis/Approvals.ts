export const Permit2 = [
  'function approve(address token, address spender, uint160 amount, uint48 expiration)'
]

export const Approve = ['function approve(address spender, uint256 amount) returns (bool)']
