import { AccountOp } from "libs/accountOp/accountOp";
import { broadcast as broadcastOnPimlico } from "services/pimlico";

export async function broadcast4337(accountOp: AccountOp) {
  // TODO:
  // 1. transform to userOperation
  // 2. broadcast to pimlico
}
